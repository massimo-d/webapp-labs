"use strict";

let arStr = [ "spring" , "porst" , "clone", "sinond" , "a" , "ac" ,"ace"];

console.log(arStr);

array.forEach((s,i) => array[i] = (s.length < 2 ) ? "" : s.substr(0, 2) + s.substr(s.length-2));

console.log(arStr);